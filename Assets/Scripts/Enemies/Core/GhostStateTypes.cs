namespace AI.Ghosts.States
{
    // Update this whenever we create a new state
    public enum GhostStateID
    {
        Idle,
        Patrol,
        Chase,
        Stunned,
        Pressure,
        Stalk,
        Hidden,
        Appear,
        Request,
        Search
    }
}